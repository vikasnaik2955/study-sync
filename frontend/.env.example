# Copy to .env for local dev. In dev these can stay relative because vite.config.js proxies
# /api and /ws to the backend on :8080. For a deployed build, point them at the real API.
VITE_API_BASE_URL=/api/v1
VITE_WS_URL=/ws
