spring:
  application:
    name: studysync-api
  datasource:
    # createDatabaseIfNotExist=true means you don't even have to create the schema by hand —
    # just have MySQL running. Override DB_USER/DB_PASS if your local root password differs.
    url: ${DB_URL:jdbc:mysql://localhost:3306/studysync?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${DB_USER:root}
    password: ${DB_PASS:root}
  jpa:
    hibernate:
      ddl-auto: update            # Hibernate owns the MySQL schema (creates/updates tables on boot)
    open-in-view: false
    properties:
      hibernate:
        jdbc.time_zone: UTC
  servlet:
    multipart:
      max-file-size: 25MB
      max-request-size: 26MB

server:
  port: ${SERVER_PORT:8080}
  error:
    include-message: never        # error detail flows through our ApiError envelope only

app:
  jwt:
    secret: ${JWT_SECRET:dev-only-change-me-to-a-long-random-256-bit-secret-value!!}
    access-ttl: ${JWT_ACCESS_TTL:PT15M}     # 15 minutes (ISO-8601 duration)
    refresh-ttl: ${JWT_REFRESH_TTL:P7D}      # 7 days
    issuer: studysync
  cors:
    allowed-origins: ${CORS_ORIGINS:http://localhost:5173}
  rate-limit:
    enabled: ${RATE_LIMIT_ENABLED:true}
    auth-capacity: 10             # requests
    auth-refill-minutes: 1        # ...per minute, per client IP
  upload:
    allowed-content-types:
      - application/pdf
      - application/msword
      - application/vnd.openxmlformats-officedocument.wordprocessingml.document
      - application/vnd.ms-powerpoint
      - application/vnd.openxmlformats-officedocument.presentationml.presentation
    max-size-bytes: 26214400      # 25 MB

storage:
  provider: ${STORAGE_PROVIDER:local}        # local | s3 | cloudinary
  local:
    dir: ${STORAGE_DIR:./var/uploads}
  signed-url-ttl: PT10M
  s3:
    bucket: ${S3_BUCKET:}
    region: ${S3_REGION:}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method

logging:
  level:
    org.springframework.security: INFO
